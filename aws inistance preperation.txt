sudo apt update
sudo apt-get install python3
sudo apt-get install python3-pip
sudo pip3 install flask
sudo apt-get install nginx
sudo apt-get install gunicorn3
Package gunicorn3 is not available, but is referred to by another package.
This may mean that the package is missing, has been obsoleted, or
is only available from another source
sudo apt-get install git
git clone https://github.com/ahmed0tolba/nawara
sudo pip3 install virtualenv 
virtualenv venv1nawara
source venv1nawara/bin/activate
pip install cmake
pip install dlib
pip3 install numpy

sudo apt-get install -y pkg-config
sudo apt-get install libxml2-dev libxslt-dev python-dev
sudo apt-get install libglib2.0-dev
sudo apt-get install libsndfile1-dev
$ sudo apt-get install libcairo2-dev
pip install git+https://github.com/patil-suraj/vqgan-jax.git
pip install git+https://github.com/borisdayma/dalle-mini.git

pip install -r requirementsubuntuawsserver.txt







